# Contact form delivery (EmailJS REST). Copy to `.env.local` and fill in.
# When any of these is missing, the contact form falls back to opening
# the visitor's mail client via `mailto:`.

VITE_EMAILJS_SERVICE_ID=
VITE_EMAILJS_TEMPLATE_ID=
VITE_EMAILJS_PUBLIC_KEY=
